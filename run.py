#!/usr/bin/env python3
"""
Скрипт для запуска гибридной системы поиска предложений музыкантов
Userbot (API_ID + API_HASH) для мониторинга + Bot (BOT_TOKEN) для уведомлений
"""

import asyncio
import sys
import os
from bot import main

def check_env_file():
    """Проверяет наличие файла .env (только для локальной разработки)"""
    if not os.path.exists('.env'):
        print("ℹ️  Файл .env не найден - используются системные переменные окружения")
        print("💡 Для локальной разработки создайте файл .env на основе .env.example")
        return False
    else:
        print("📄 Найден файл .env - будет использован для локальной разработки")
        return True

def run():
    """Главная функция запуска"""
    print("🎵 Запуск гибридной системы поиска предложений музыкантов...")
    print("👤 Userbot (API_ID + API_HASH) - мониторинг групп")
    print("🤖 Bot (BOT_TOKEN) - отправка уведомлений")
    print("🛡️  Добавлены задержки для безопасности")
    print("📋 Все данные берутся из переменных окружения")
    print("⚠️  При первом запуске userbot может запросить код подтверждения")
    
    # Проверяем конфигурацию
    check_env_file()
    
    try:
        asyncio.run(main())
    except KeyboardInterrupt:
        print("\n⏹️  Получен сигнал остановки (Ctrl+C)")
        print("🔄 Завершение работы...")
    except Exception as e:
        print(f"❌ Критическая ошибка: {e}")
        sys.exit(1)

if __name__ == "__main__":
    run()