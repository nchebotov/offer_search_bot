# Offer Search Bot

Telegram бот для поиска предложений музыкантов в группах по ключевым словам.

## Описание

**Гибридная система** мониторинга Telegram групп:
- **Userbot** (API_ID + API_HASH) - мониторит группы от вашего имени
- **Bot** (BOT_TOKEN) - отправляет уведомления в целевую группу

Система отслеживает сообщения в реальном времени, фильтрует по ключевым словам (музыкальные инструменты, жанры, мероприятия) и отправляет уведомления с информацией об авторе, источнике и кнопками для связи.

## Особенности

- ✅ **Гибридная архитектура** - userbot + bot для максимальной эффективности
- ✅ **Событийная модель** - мониторинг в реальном времени
- ✅ **Безопасность** - случайные задержки для имитации человеческого поведения
- ✅ Конфигурация через **переменные окружения**
- ✅ Легко настраивается для развертывания на хостинге
- ✅ Автоматическое извлечение контактной информации
- ✅ Умные кнопки для связи с авторами
- ✅ Детальная информация о найденных предложениях
- ✅ Логирование всех операций

## Функционал

- 🔍 Мониторинг множественных Telegram групп в реальном времени
- 🎯 Фильтрация сообщений по ключевым словам
- 📤 Отправка уведомлений в целевую группу
- 🔗 Автоматическое создание кнопок для связи
- 🌐 Ссылки на группу и конкретное сообщение
- ⚙️ Гибкая конфигурация через .env файл
- 📊 Подробное логирование работы
- ⚡ События вместо polling - мгновенная реакция

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd offer_search_bot
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните переменные окружения в файле `.env`

## Конфигурация

### Получение API_ID и API_HASH

1. Перейдите на https://my.telegram.org/
2. Войдите в свой аккаунт Telegram
3. Перейдите в "API development tools"
4. Создайте новое приложение
5. Скопируйте `api_id` и `api_hash`

### Создание бота и получение BOT_TOKEN

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

### Настройка целевой группы

1. Создайте группу для уведомлений
2. Добавьте вашего бота в эту группу как администратора
3. Укажите username группы в TARGET_GROUP (например: @your_target_group)

### Файл .env

```env
# Telegram API credentials
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890

# Bot token от @BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Целевая группа для уведомлений
TARGET_GROUP=@your_target_group

# Session name
SESSION_NAME=offer_search_bot

# Группы для мониторинга (через запятую, опционально)
GROUPS_TO_MONITOR=t.me/eventori,t.me/bigfingerchat,t.me/meropriyatiyachat

# Ключевые слова (через запятую, опционально)
KEYWORDS=пианист,гитарист,скрипач,вокалист,музыкант,оркестр,концерт
```

## Конфигурация для хостинга

### Переменные окружения

Для развертывания на хостинге (Heroku, Railway, etc.) установите следующие переменные окружения:

**Обязательные:**
- `API_ID` - API ID от my.telegram.org
- `API_HASH` - API Hash от my.telegram.org  
- `BOT_TOKEN` - Токен бота от @BotFather
- `TARGET_GROUP` - Целевая группа для уведомлений

**Опциональные:**
- `SESSION_NAME` - Имя сессии (по умолчанию: offer_search_bot)
- `GROUPS_TO_MONITOR` - Список групп через запятую
- `KEYWORDS` - Список ключевых слов через запятую

### Дефолтные списки

Если `GROUPS_TO_MONITOR` или `KEYWORDS` не заданы, используются встроенные списки:

**Ключевые слова по умолчанию:**
- Инструменты: пианист, гитарист, скрипач, саксофонист...
- Коллективы: оркестр, квартет, кавер-бэнд, хор...
- Жанры: джаз, классика, рок, поп, живая музыка...

## Запуск

```bash
# Основной запуск
python run.py

# Или напрямую
python bot.py
```

## Как работает система

### 🔄 **Гибридная архитектура:**

**Userbot (мониторинг):**
- Использует ваши API_ID + API_HASH
- Работает от вашего имени
- Имеет доступ ко всем группам где вы участник
- Отслеживает новые сообщения через события

**Bot (уведомления):**
- Использует BOT_TOKEN от @BotFather
- Отправляет уведомления в целевую группу
- Создает кнопки и ссылки

### 📋 **Процесс работы:**

1. **Инициализация**: Запуск userbot и bot клиентов
2. **Авторизация**: При первом запуске userbot запросит номер телефона
3. **Настройка**: Получение информации о группах для мониторинга
4. **События**: Настройка обработчиков новых сообщений
5. **Мониторинг**: Userbot отслеживает сообщения в реальном времени
6. **Фильтрация**: Анализ текста на наличие ключевых слов
7. **Безопасность**: Случайные задержки между действиями
8. **Уведомления**: Bot отправляет найденные сообщения в целевую группу

## Навигация в уведомлениях

### Кнопка "Написать"
Бот автоматически создает кнопку для связи с автором:

1. **Если в тексте есть @username** - кнопка ведет на этот username
2. **Если у автора есть username** - кнопка ведет на username автора
3. **Если username нет** - кнопка создает прямую ссылку на пользователя по ID

### Ссылки на источник
Каждое уведомление содержит:

- **Ссылка на группу** - клик по названию группы ведет в саму группу
- **Ссылка на сообщение** - "Перейти к сообщению" ведет к конкретному сообщению
- **Поддержка приватных групп** - работает с группами по ID

### Пример уведомления:
```
🎵 Найдено предложение!

👤 Автор: @musician
💬 Группа: Event Chat (кликабельная ссылка)
🔍 Ключевые слова: пианист, концерт
📅 Время: 04.11.2024 20:30

Сообщение:
Ищу пианиста на корпоратив...

🔗 Перейти к сообщению (кликабельная ссылка)
👆 Написать @musician (кликабельная кнопка)
```

## Требования

- Python 3.7+
- Telegram API credentials
- Bot token от @BotFather
- Бот должен быть добавлен в целевую группу и группы для мониторинга

## Устранение неполадок

### Ошибка "UserNotParticipant"
- Бот должен быть добавлен во все группы для мониторинга
- Бот должен быть добавлен в целевую группу

### Ошибка "ChatAdminRequired"  
- Боту нужны права администратора в целевой группе
- В некоторых группах для мониторинга могут потребоваться особые права

### Ошибка "FloodWait"
- Telegram ограничивает частоту запросов
- Бот автоматически ждет необходимое время

## Структура проекта

```
offer_search_bot/
├── bot.py              # Основная логика бота (Telethon)
├── config.py           # Конфигурация через переменные окружения
├── run.py              # Скрипт запуска
├── requirements.txt    # Зависимости
├── .env.example       # Пример переменных окружения
├── .env               # Ваши переменные окружения (создать)
├── README.md          # Документация
└── QUICKSTART.md      # Быстрый старт
```

## Безопасность

- Никогда не публикуйте файл `.env` с вашими токенами
- Используйте переменные окружения на продакшене
- Регулярно обновляйте зависимости
- Бот работает только с публичными сообщениями в группах

## Лицензия

MIT License